langcode: en
status: true
dependencies:
  module:
    - labdoo_dootrip
title: 'Queue: Calculate the total CO2 savings'
id: ultimate_cron_queue_labdoo_dootrip_calculate_total_co2_savings
weight: 10
module: labdoo_dootrip
callback: 'ultimate_cron.queue_worker:queueCallback'
scheduler:
  id: simple
  configuration:
    rules:
      - '* * * * *'
launcher:
  id: serial
logger:
  id: database
