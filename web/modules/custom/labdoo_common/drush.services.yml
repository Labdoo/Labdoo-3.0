services:
  labdoo_common.commands:
    class: \Drupal\labdoo_common\Commands\GeocodeCommands
    arguments: ['@entity_type.manager', '@entity_field.manager', '@labdoo_common.geocode_cache_manager']
    tags:
      - { name: drush.command }
